<div class="item_heading">
    <span class="image">
        <img class="icon heading_icon" src="{{iconUrl}}" alt="job header"/>
    </span>
    <h1 title="{{name}}">{{name}}</h1>
</div>


<div class="job_information">
<div class="item_metadata">
    <div>
        {{#if frequency}}
            <span class="frequency">
                <span class="label">{{t "job.run_frequency.label"}}</span>
                {{frequency}}
            </span>
        {{/if}}

        {{#if nextRun}}
            <span class="next_run">
                <span class="label">{{t "job.next_run"}}</span>
                {{displayTimestamp nextRun}}
            </span>
        {{/if}}

        <span class="owner pull-right">
            <span class="label">{{t "job.owner"}}</span>
            <a href='{{ownerUrl}}'>{{ownerName}}</a>
        </span>
    </div>
    
    <div>
        <span class="last_run">
           <span class="label">{{t "job.last_run"}}</span>
            {{#if lastRun}}
                <a class="last_run_date" href="#" title="{{t 'job.show_details.tooltip'}}">{{displayTimestamp lastRun}}. <span class="status">{{t lastRunStatusKey}}</span></a>
            {{else}}
                {{t "job.last_run_no_last_run"}}
            {{/if}}
        </span>
    </div>
</div>
</div>